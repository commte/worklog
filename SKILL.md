---
name: log
description: 当日の作業ログを記録する。/log で実行。会話の内容から作業内容を抽出し、private/作業ログ/YYYY-MM-DD.md に記録する。作業の区切りや終了時に使う。
---

# /log スキル

当日の作業ログを `private/作業ログ/YYYY-MM-DD.md` に記録する。
置き場所を変えたいプロジェクトは、その CLAUDE.md に「作業ログは `docs/log/` に置く」のように書く。
そちらが優先。

## 手順

### 1. 今日の日付を取得

```bash
date +%Y-%m-%d
```

### 2. 既存ログの確認

`private/作業ログ/YYYY-MM-DD.md` が既に存在するか確認する。

- 存在する場合: 既存の内容を読み込み、末尾に追記する
- 存在しない場合: 新規作成する

### 3. 会話から作業内容を抽出

現在の会話の内容から、以下の情報を抽出する:

- 何を実装・修正したか（ファイルパス付き）
- 重要な意思決定や設計判断
- 未完了のまま持ち越した作業

### 4. ログの記述フォーマット

新規作成の場合:

```markdown
# YYYY-MM-DD 作業ログ

## セクション名（機能名やカテゴリ）

### 小見出し

- 作業内容（ファイルパス付き）
- 作業内容

## 意思決定

- 判断した内容とその理由

## 次回タスク

- 持ち越した作業
```

追記の場合は、既存のセクション構造に合わせて追記する。
重複する内容は書かない。

### 5. 記述ルール

- 箇条書きで簡潔に書く
- ファイルパスはバッククォートで囲む
- Markdown の強調記法（`**太字**`）は使用禁止
- Markdown の区切り線（`---`）は使用禁止
- 絵文字は使用しない
